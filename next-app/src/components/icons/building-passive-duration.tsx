import { LucideIconProps } from "@/types/icons";
import { AlarmClockPlus } from "lucide-react";

export const BuildingPassiveDurationIcon = ({ ...props }: LucideIconProps) => {
  return <AlarmClockPlus {...props} />;
};
