import { LucideIconProps } from "@/types/icons";
import { Calculator } from "lucide-react";

export const CapacityCalculatorIcon = ({ ...props }: LucideIconProps) => {
  return <Calculator {...props} />;
};
