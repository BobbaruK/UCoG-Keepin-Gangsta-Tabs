import { LucideIconProps } from "@/types/icons";
import { Landmark } from "lucide-react";

export const BuildingPassiveIcon = ({ ...props }: LucideIconProps) => {
  return <Landmark {...props} />;
};
