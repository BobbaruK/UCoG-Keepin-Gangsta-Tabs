import { LucideIconProps } from "@/types/icons";
import { HousePlus } from "lucide-react";

export const BuildingBackroomIcon = ({ ...props }: LucideIconProps) => {
  return <HousePlus {...props} />;
};
