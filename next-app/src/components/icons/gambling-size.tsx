import { LucideIconProps } from "@/types/icons";
import { WalletCards } from "lucide-react";

export const GamblingSizeIcon = ({ ...props }: LucideIconProps) => {
  return <WalletCards {...props} />;
};
